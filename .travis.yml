language: python
python:
  - "3.5"
  - "3.6"

install:
  - 'pip install pipenv'
  - 'pip install python-coveralls'
  - 'pipenv install --dev'

script:
  - pytest --cov=PyTCI

after_success:
  - coveralls